import 'package:flutter/material.dart';
import 'package:bmi_calclulator/input_page.dart'; // Use the suggestion list to import
void main()
{
  runApp(
    MaterialApp(
      home: InputPage(),
    )); // When ever our app starts, it will triger the input page, so we need to define an input page in  seperate file
}

